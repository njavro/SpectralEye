# Real Sionna RT coverage backend (Colab notebook + cloudflared tunnel).
# Leave UNSET to fall back to MockSionnaSource (analytical free-space model;
# fast, runs locally, no GPU required).
#
# When set, every coverage request goes to this URL — see colab/README.md
# for how to spin up the notebook and get a URL.
#
# SIONNA_REMOTE_URL=https://random-words-1234.trycloudflare.com
